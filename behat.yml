default:
    extensions:
        Laracasts\Behat:
            # env_path: .env.behat
        Behat\MinkExtension:
            default_session: laravel
            laravel: ~
    suites:
        candidate_features:
            paths:      [ %paths.base%/features/candidate ]
            contexts:   [ CandidateContext ]
            
        recruiter_features:
            paths:      [ %paths.base%/features/recruiter ]
            contexts:   [ RecruiterContext ]